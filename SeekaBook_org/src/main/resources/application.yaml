server:
  port: 8081
  servlet:
    context-path: /seekabook
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seekabook?autoreconnect = true
    username: root
    password: root